services:
  test:
    container_name: test
    platform: linux/amd64
    pid: "host"
    build:
      context: .
      dockerfile: .docker/test-integration/Dockerfile
    depends_on:
      - app
    networks:
      - app

networks:
  app:
    driver: bridge
