apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - name: grpc
      port: {{ .Values.app.grpc.port }}
    - name: http
      port: {{ .Values.app.diagnostic.port }}