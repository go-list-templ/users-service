apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - name: grpc
      port: {{ .Values.service.grpcPort }}
    - name: http
      port: {{ .Values.service.diagPort }}